# Input Evidence

Store sanitized or access-controlled evidence used to compare the real BitSight alert API with the web UI.

Do not store BitSight or Jira API keys, browser cookies, authorization headers, or session tokens here.
